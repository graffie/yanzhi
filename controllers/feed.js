/**!
 * 5kw2 - controllers/feed.js
 */

'use strict';

/**
 * Module dependencies.
 */

// var Feed = require('../proxy/feed');

exports.index = function* (next) {
  // this.verifyParams({
  //   projectId: {isId: true}
  // });

  // this.body = yield Period.get(this.params.projectId);
};

exports.create = function* (next) {
  // this.verifyParams({
  //   projectId: {isId: true},
  //   title: 'string',
  //   dateStart: {isDate: true},
  //   dateEnd: {isDate: true},
  //   color: 'string',
  //   height: /^\d+$/
  // });

  // var period = {};
  // period.projectId = this.params.projectId;
  // period.userId = this.user.id;
  // period.title = this.request.body.title;
  // period.dateStart = this.request.body.dateStart;
  // period.dateEnd = this.request.body.dateEnd;
  // period.color = this.request.body.color;
  // period.height = this.request.body.height;

  // var res = yield Period.add(period);
  // period.id = res.insertId;
  // this.body = period;
  // this.status = 201;
};
